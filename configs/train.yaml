# conf/train.yaml
seed: 42

# Data
data:
  root: data/raw
  batch_size: 32
  num_workers: 0        
  pin_memory: true
  img_size: 224

# Training
epochs: 15
amp_dtype: "fp16" 
save_best_by: "val_acc" 


# Other
log_interval: 50        
