# configs/train.yaml
seed: 42
epochs: 20
amp_dtype: "fp16" 
save_best_by: "val_macro_f1" 
log_interval: 10
grad_clip_norm: 1.0
debug_data_checks: false
hash_data_checks: false

early_stopping:
  patience: 5
  min_delta: 1e-4

scheduler:
  factor: 0.5
  patience: 2
  min_lr: 1e-6

